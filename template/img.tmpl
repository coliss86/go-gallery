<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>Parcours du dossier {{.Title}}</title>
		<script type="text/javascript" src="static/js/jquery-1.10.1.min.js"></script>
		<script type="text/javascript" src="static/js/jquery.fancybox-2.1.5.pack.js"></script>
		<script type="text/javascript" src="static/js/webcam.js"></script>
		<link rel="stylesheet" type="text/css" href="static/css/jquery.fancybox.css" media="screen" />
		<link rel="stylesheet" type="text/css" href="static/css/webcam.css" media="screen" />
	</head>
	<body>
		<div class="title">Parcours du dossier {{.Title}}</div>
		{{if .Folders}}
		<div class="folders">
			{{range .Folders}}<div><a href="web.go?folder={{ . }}">{{ . }}</a></div>
			{{end}}
		</div>
		{{end}}
		{{if .Pictures}}
		<div class="pictures">
			{{range .Pictures}}<a class="fancybox" rel="group" href="/img/{{$.Title}}/{{ . }}" title="{{ . }}"><img src="/img/{{$.Title}}/{{ . }}" alt="" width="50px" height="50px" /></a>
			{{end}}
		</div>
		{{end}}
	</body>
</html>
